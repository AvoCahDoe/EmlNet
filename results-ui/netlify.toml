# Static Vite app — not Next.js. If the site has @netlify/plugin-nextjs enabled
# in the Netlify UI, this skips it so deploy uses `dist/` only.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
